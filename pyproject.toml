[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[dependency-groups]
build = [
    "build",
]
dev = [
    "nox",
    "nox-uv",
    {include-group = "build"},
    {include-group = "lint"},
]
docs = [
    "mkdocstrings",
    "mkdocstrings-python",
    "zensical",
]
lint = [
    "prek",
]

[project]
name = "ready"
dynamic = ["version", "readme"]
requires-python = ">=3.12" #python=3.12 local  #python3.10 apptainer
keywords = ["AI"]
authors = [
  {name = "Miguel Xochicale", email = "add@email.com"},
  {name = "Add co-author", email = "add@email.com"},
]
maintainers = [
  {name = "Add maintener", email = "add@email.com"}
]
description = "Package example!"
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python"
]
dependencies = [
    "numpy",
    "matplotlib",
    "pandas",
    "PyQt6",
    "loguru",
    "omegaconf",
    "scikit-learn",
    "pydot",
]
[project.optional-dependencies]
# development dependency groups
test = [
    "pytest",
    "pylint",
    "black",
    "isort",
    "codespell",
    "pre-commit"
]
learning = [
    "torch", #2.13.0 local #cricket PyTorch Version 2.3.0a0+6ddf5cf
    "torchvision", #0.28.0 local
]
model_optimisation = [
    "onnx", #1.22.0 local
    "onnxruntime",#1.28.0 local
    "onnxsim",#0.7.1 local
    "onnxscript", #0.7.1 local
    "onnx-ir", #0.2.1 local
]

[tool.setuptools.dynamic]
version = {attr = "ready.__version__"}
readme = {file = ["README.md"]}

[tool.setuptools.packages.find]
where = ["src"]  # list of folders that contain the packages (["."] by default)
include = ["ready"]  # package names should match these glob patterns (["*"] by default)
#exclude = ["my_package.tests*"]  # exclude packages matching these glob patterns (empty by default)
namespaces = false  # to disable scanning PEP 420 namespaces (true by default)
